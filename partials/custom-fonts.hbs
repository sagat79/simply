{{#if @site.customFonts}}
    {{#with @site.customFonts}}
        <style>
            :root {
                --font-title: {{title.default}};
                --font-body: {{body.default}};
            }
        </style>
    {{/with}}
{{/if}} 